/**
 * Dependency injection
 *
 * <p>Java Dependency Injection design pattern allows us to remove the hard-coded dependencies and
 * make our application loosely coupled, extendable and maintainable. We can implement dependency
 * injection in java to move the dependency resolution from compile-time to runtime.
 */
package com.inspire17.tutorial.lld.strucuralpattern.di;
